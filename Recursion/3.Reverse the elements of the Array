#include<iostream>
using namespace std;
void reverse(int arr[],int size){
    if(size==0 || size==1)
    return;
    int temp = arr[0];
    arr[0]=arr[size-1];
    arr[size-1]=temp;
    reverse(arr+1,size-2);
}
int main(){
    int arr[5];
    for(int i=0;i<5;i++)
    cin >> arr[i];
    reverse(arr,5);
    for(int i=0;i<5;i++)
    cout << arr[i] << " ";
    return 0;
}
